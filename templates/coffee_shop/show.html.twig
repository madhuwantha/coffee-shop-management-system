{% extends 'base.html.twig' %}

{% block title %}CoffeeShop{% endblock %}

{% block body %}
    <h1>CoffeeShop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coffee_shop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coffee_shop.name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ coffee_shop.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coffee_shop_index') }}">back to list</a>

    <a href="{{ path('coffee_shop_edit', {'id': coffee_shop.id}) }}">edit</a>

    {{ include('coffee_shop/_delete_form.html.twig') }}
{% endblock %}

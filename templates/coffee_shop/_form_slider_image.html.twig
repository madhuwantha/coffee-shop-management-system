
<div class="form-group row">
    <label for="sibling_name"
           class="col-sm-4 control-label">Path</label>
    <div class="col-sm-8">
        {{ form_widget(slider.file, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(slider.file) }}
    </div>
</div>

<div class="form-group row">
    <label for="sibling_name"
           class="col-sm-4 control-label">Slider Position</label>
    <div class="col-sm-8">
        {{ form_widget(slider.position, {'type':'number', 'attr': {'class': 'form-control'}}) }}
        {{ form_errors(slider.position) }}
    </div>
</div>
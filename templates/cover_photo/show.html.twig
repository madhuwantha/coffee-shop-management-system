{% extends 'base.html.twig' %}

{% block title %}CoverPhoto{% endblock %}

{% block body %}
    <h1>CoverPhoto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cover_photo.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ cover_photo.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cover_photo_index') }}">back to list</a>

    <a href="{{ path('cover_photo_edit', {'id': cover_photo.id}) }}">edit</a>

    {{ include('cover_photo/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('contact_details_delete', {'id': contact_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_detail.id) }}">
    <button class="btn">Delete</button>
</form>

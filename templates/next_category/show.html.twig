{% extends 'base.html.twig' %}

{% block title %}NextCategory{% endblock %}

{% block body %}
    <h1>NextCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ next_category.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('next_category_index') }}">back to list</a>

    <a href="{{ path('next_category_edit', {'id': next_category.id}) }}">edit</a>

    {{ include('next_category/_delete_form.html.twig') }}
{% endblock %}

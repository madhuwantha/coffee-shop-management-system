{% extends 'base.html.twig' %}

{% block title %}GalleryVideo{% endblock %}

{% block body %}
    <h1>GalleryVideo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gallery_video.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gallery_video.name }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ gallery_video.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gallery_video_index') }}">back to list</a>

    <a href="{{ path('gallery_video_edit', {'id': gallery_video.id}) }}">edit</a>

    {{ include('gallery_video/_delete_form.html.twig') }}
{% endblock %}

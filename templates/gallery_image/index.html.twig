{% extends 'base.html.twig' %}

{% block title %}GalleryImage index{% endblock %}

{% block body %}
    <h1>GalleryImage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Path</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gallery_image in gallery_images %}
            <tr>
                <td>{{ gallery_image.id }}</td>
                <td>{{ gallery_image.name }}</td>
                <td>{{ gallery_image.path }}</td>
                <td>
                    <a href="{{ path('gallery_image_show', {'id': gallery_image.id}) }}">show</a>
                    <a href="{{ path('gallery_image_edit', {'id': gallery_image.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gallery_image_new') }}">Create new</a>
{% endblock %}

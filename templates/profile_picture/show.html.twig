{% extends 'base.html.twig' %}

{% block title %}ProfilePicture{% endblock %}

{% block body %}
    <h1>ProfilePicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile_picture.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profile_picture_index') }}">back to list</a>

    <a href="{{ path('profile_picture_edit', {'id': profile_picture.id}) }}">edit</a>

    {{ include('profile_picture/_delete_form.html.twig') }}
{% endblock %}
